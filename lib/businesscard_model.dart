import 'package:flutter/cupertino.dart';

class BusinessCard {
  final String name, position, phonenumber, email, workaddress, contactaddress;

  BusinessCard(this.name, this.position, this.phonenumber, this.email,
      this.workaddress, this.contactaddress);
}
